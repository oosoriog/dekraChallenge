# assessment-tech-plan

This skill creates implementation-ready technical plans for phase-based Java/Spring Boot development.

It is designed to run after `assessment-research-plan`.

It reads the repository AI context, inspects the current repository, asks for approval when decisions must change, and updates planning documents only when allowed.

It never implements code.
